        location ~ [^/]\.php(/|$)
        {
            try_files $uri =404;
            fastcgi_pass  unix:/usr/local/php8.2/var/run/php-fpm.sock;
            fastcgi_index index.php;
            include fastcgi.conf;
        }
